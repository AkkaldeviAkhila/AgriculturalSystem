services:
  - type: web
    name: smart-agriculture-system
    runtime: python
    env: python
    pythonVersion: 3.10
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py

